<%= form_for(@kitten) do |form| %>
  <% if kitten.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kitten.errors.count, "error") %> prohibited this kitten from being saved:</h2>

      <ul>
      <% kitten.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= form.text_field :name,     placeholder: "What's your kitten's name?" %><br />
  <%= form.text_field :age,      placeholder: "How old is your kitten?" %><br />
  <%= form.text_field :cuteness, placeholder: "How cute is your kitten?" %><br />
  <%= form.text_field :softness, placeholder: "How soft is your kitten?" %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
